# Todo

- [ ] **Sub-Agent 实现**: 设计并实现 `spawn_agent` 工具，支持主 Agent 创建上下文隔离的子代理。子代理拥有独立的 system prompt，不访问父对话上下文，运行结束后将结构化结果返回给主 Agent。需要：定义子代理的工具集（可能是主代理工具的子集）、上下文传递协议、结果格式规范。Spec 中已有设计原则，参考 `docs/superpowers/specs/2025-04-17-ai-task-assistant-design.md` §2 "Sub-Agent" 节。
